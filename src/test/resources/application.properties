#catalog - load from GitHub (same as production) so tests don't rely on local files
centerstage.catalog.file=https://github.com/goafabric/backstage/blob/develop/catalog/entities-git.yaml

#ingestion schedule - long interval so scheduler fires only once (at delay=0) during tests
centerstage.ingestion.interval=PT24H

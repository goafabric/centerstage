#catalog - load from local files so tests don't depend on GitHub availability
centerstage.catalog.file=src/test/resources/catalog/entities-local.yaml

#ingestion schedule - long interval so scheduler fires only once (at delay=0) during tests
centerstage.ingestion.interval=PT24H
